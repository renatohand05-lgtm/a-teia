import { ConnectionsPreview } from "@/components/cockpit/ConnectionsPreview";

/** Resíduo V9 removido. O mapa ilustrativo não entra no Release 1.0. */
export function NetworkMap() {
  return <ConnectionsPreview companies={[]} />;
}
